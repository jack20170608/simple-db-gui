name = peanotes

server = {
  port=3333
  context-path="db-viewer"
  name=db-viewer
}


# the database config
database = {
  ready=false
  driver="org.postgresql.Driver"
  url="NOT-SET"
  user="NOT-SET"
  password="NOT-REQUIRED"
  auto_commit=true
}


